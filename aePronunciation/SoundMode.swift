

enum SoundMode: String {
    // Don't change the hardcoded strings unless you take into
    // consideration that they may be used in persistent memory.
    case single = "single"
    case double = "double"
}
